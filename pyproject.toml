[project]
name = "demo-1-0"
version = "0.1.0"
description = "Demo 1.0 project"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "Demo User" }]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
