# 舆情全链路智能处置工作台 · MVP 产品设计文档

> 版本：v1.0（重新实现版）　·　原则：**全用真的，砍掉一切假的**

## 1. 产品定位

一个「全真接入」的舆情处置工具，覆盖 **采集 → 核查 → 评论 → 投诉 → 报告** 全链路。
每一步都由真实大模型（DeepSeek）驱动，作为简历 / 作品集项目。

## 2. 核心原则

1. **全真接入**：所有 AI 能力真调 DeepSeek，不装假、不降级成假 AI。
2. **弱采集、重处置**：采集只是「喂数据」的入口，重心放在后面四个处置环节。
3. **无 Key 即引导**：未配 Key 时明确引导用户配置，不假装智能、不显示写死结果。
4. **单一数据源**：只接真实 RSS 新闻，砍掉写死的假数据源。

## 3. 信息架构（全链路 5 模块）

| 模块 | 定位 | 权重 |
|---|---|---|
| ① 舆情采集 | 数据入口（弱化） | 低 |
| ② AI 核查 | 真实性研判 | 高 |
| ③ AI 事实评论 | 回应口径生成 | 高 |
| ④ AI 投诉处置 | 分类 / 转办 / 话术 | 高 |
| ⑤ 处置报告 | 汇总 + 留痕 + 导出 | 高 |

## 4. 各模块功能

### ① 舆情采集（弱化）
- 聚合 RSS 真实新闻，列表展示（标题 / 来源 / 时间 / 情感）
- 情感标注：DeepSeek 批量判定；失败降级关键词规则，并**明确标注判定方式**
- 点一条 →「选为研判对象」，成为后续所有模块的处理目标
- 不做：多数据源切换、复杂筛选、自建爬虫

### ② AI 核查（重点）
- 输入：当前研判对象（一条舆情）
- DeepSeek 输出：核查结论（真 / 假 / 存疑）+ 证据链 + 风险等级 + 处置建议
- 单次调用出结论（不做多轮交互审问）

### ③ AI 事实评论（重点）
- 输入：研判对象 + 评论风格（官方权威 / 专业严谨 / 亲民温暖）
- DeepSeek 输出：符合风格、实事求是、不传谣不信谣的回应口径

### ④ AI 投诉处置（重点）
- 输入：一段投诉文本
- DeepSeek 输出：分类 + 优先级 + 转办部门 + 回复话术（结构化）

### ⑤ 处置报告（重点）
- 一键汇总本次全链路处置过程，生成报告
- 处置留痕：每一步处置自动归档（SQLite 持久化，重启不丢）
- 导出：报告一键下载为 `.md` 文件

## 5. 无 Key 时的行为

- 检测到未配置 Key → 页面明确提示「请配置 DeepSeek Key」，并提供配置入口
- 不降级到假 AI、不显示写死的示例结果

## 6. 技术方向

- 纯前端（HTML / JS / CSS）+ Python 标准库代理（`server.py`），零第三方依赖
- DeepSeek 经本地代理调用（保护 Key + 解决跨域）
- Key 配置：网页填写（存服务端）+ 环境变量，**不写死在代码**
- 持久化：SQLite（处置留痕）
- 部署：Cloudflare Pages（无需 GitHub / 无需绑卡）

## 7. 明确不做（本期）

- ❌ 模拟大脑 MockLLM（假 AI）
- ❌ 清博 / 人民众云（写死的假数据源）
- ❌ 交互式多轮审问
- ❌ 用户登录 / 多租户
- ❌ 图表可视化（留待后续迭代）

## 8. 后续规划（简要）

- P1：情感 / 趋势可视化、高热度自动预警
- P2：接入更多真实数据源、正式部署上线
